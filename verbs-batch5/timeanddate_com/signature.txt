# Looks up the current time for a city on timeanddate.com. Extracts the city
# name, current time, date, timezone abbreviation, and UTC offset.

@dataclass(frozen=True)
class TimeRequest:
    city: str = "Tokyo"

@dataclass
class TimeResult:
    city_name: str = ""
    current_time: str = ""
    date: str = ""
    timezone_abbr: str = ""
    utc_offset: str = ""

def time_lookup(page: Page, request: TimeRequest) -> TimeResult:
