# Searches Poshmark for secondhand fashion listings matching a query. For each
# listing, extracts the title and URL.

@dataclass(frozen=True)
class ListingRequest:
    query: str = "designer handbag Louis Vuitton"
    max_results: int = 5

@dataclass
class Listing:
    title: str = ""
    url: str = ""

@dataclass
class ListingResult:
    items: List[Listing] = field(default_factory=list)

def poshmark_search(page: Page, request: ListingRequest) -> ListingResult:
