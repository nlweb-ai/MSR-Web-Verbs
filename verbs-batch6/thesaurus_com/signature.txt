# Searches Thesaurus.com for synonyms and word entries matching a query.
# For each result, extracts the title and URL.

@dataclass(frozen=True)
class WordRequest:
    query: str = "synonyms for happy"
    max_results: int = 5

@dataclass
class Word:
    title: str = ""
    url: str = ""

@dataclass
class WordResult:
    items: List[Word] = field(default_factory=list)

def thesaurus_search(page: Page, request: WordRequest) -> WordResult:
