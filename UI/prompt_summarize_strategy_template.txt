Please analyze the following function and create a clear, concise summary that explains:
1. The user is non-technical (e.g., grandpa or grandma), so avoid any programming jargon.
2. What the overall goal/task is
3. The main steps performed (in order)
4. What data is being collected or processed
5. Whenever a web verb is used (i.e., a function imported from the verbs/ folder such as search_amazon_products, search_nearby, etc.), display the verb name formatted as inline code using backticks — e.g., `walmart_com/walmart_search` or `maps_google_com__nearby/maps_nearby`. Use the folder/module name, not the function name.

Here is the function:

```python
{automate_function}
```

Please provide the summary in markdown format with clear headings and bullet points.

IMPORTANT: Wrap your entire output between the exact markers ===START_MD=== and ===END_MD=== (do NOT use ``` code fences). For example:
===START_MD===
## Summary
...
===END_MD===